import { NameOrderPipe } from './name-order.pipe';

describe('NameOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new NameOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
