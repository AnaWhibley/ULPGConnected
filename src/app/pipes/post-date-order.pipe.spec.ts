import { PostDateOrderPipe } from './post-date-order.pipe';

describe('PostDateOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new PostDateOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
